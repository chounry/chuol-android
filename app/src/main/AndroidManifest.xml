<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.group6.choul">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".login_register_handling.MyApp"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <!--<activity android:name=".login_register_handling.RegisterActivity"/>-->
        <activity android:name=".MapFormActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyB1dbxszCAqSCrePrLOveBD1XWaBHIM8PU" />

        <activity android:name=".HouseDetailActivity" />
        <activity android:name=".RoomDetailActivity" />
        <activity android:name=".PostHistoryActivity" />
        <activity android:name=".ChatInActivity" />
        <activity android:name=".HouseFormActivity" />
        <activity android:name=".RoomFormActivity" />

        <activity android:name=".RegisterActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

</application>

</manifest>